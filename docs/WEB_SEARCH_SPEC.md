# Web Search Feature Specification
# 网络搜索功能规范文档

**Version:** 2.0 (Python + Tavily + DeepSeek)  
**Last Updated:** 2025-12-20  
**Status:** Under Development

---

## 1. Overview | 概述

### 1.1 Purpose | 目的
网络搜索功能为 MeetWith AI 提供实时网络信息获取能力，使 AI 能够回答需要最新信息的问题，突破预训练知识的时间限制。

使用 **Tavily Search API**提供搭查功能，使用 **DeepSeek 模型**提供 AI 阻理能力，集成在 **FastAPI + asyncio** 中。

### 1.2 Scope | 范围
本规范涵盖从用户消息分析、搜索决策、执行搜索、内容提取到上下文构建的完整流程，但**不包括** Deep Research 功能。

### 1.2 Scope | 范围
本规范涵盖从用户消息分析、搜索决策、执行搜索、内容提取到上下文构建的完整流程，但**不包括** Deep Research 功能（Deep Research 使用 Interactions API，是独立的研究系统）。

### 1.3 Objectives | 目标
- ✅ 自动判断何时需要网络搜索
- ✅ 智能迭代搜索，最多 3 轮
- ✅ 提取並排序相关内容
- ✅ 提供实时进度反馈（SSE）
- ✅ 内存存储，暂无频率限制

---

## 2. Functional Requirements | 功能需求

### 2.1 Prompt Analysis | 提示词分析

#### FR-2.1.1 自动判断需求
**优先级:** P0 (Critical)

系统必须能够自动分析用户输入，判断是否需要网络搜索。

**触发条件:**
- 用户明确要求 "latest", "current", "recent", "今天", "最新"
- 实时数据需求：股票价格、天气、汇率、加密货币
- 时间敏感信息：2024-2025 年的事件、比较、统计
- 可能过时的信息：公司状态、政策、技术版本

**排除条件:**
- 通用知识：算法、设计模式、历史事实（2023 年前）
- 创意任务：故事创作、代码生成（无需外部数据）
- 哲学讨论、数学证明

**实现方式:**
使用 Gemini Flash Lite 模型分析用户输入，返回结构化的 `PromptAnalysisResult`。

#### FR-2.1.2 生成初始搜索查询
系统必须根据用户输入生成优化的搜索查询。

**要求:**
- 简洁（5-10 词）
- 包含关键实体和时间限定词
- 英文优先（Google 搜索效果更好）

**示例:**
| 用户输入 | 生成查询 |
|---------|---------|
| "今天北京天气怎么样？" | "Beijing weather today" |
| "比较 2024 年的 iPhone 16 和 Samsung S24" | "iPhone 16 vs Samsung S24 2024" |

#### FR-2.1.3 识别目标信息
系统必须识别用户想要了解的具体方面（targetInfo）。

**示例:**
- 查询: "特斯拉最新财报"
- targetInfo: ["revenue", "profit", "stock price", "guidance"]

---

### 2.2 Web Search Execution | 网络搜索执行

#### FR-2.2.1 Tavily Search Integration
**优先级:** P0

使用 Tavily Search API 执行搜索。

**配置:**
- 每次搜索返回 **5-10 个结果**
- 支持自定义搜索参数
- API Key 通过环境变量配置

Tavily 优势:
- 包含每个结果的简述
- 支持原始内容违s转
- 更安定的 API

#### FR-2.2.2 Rate Limiting | 速率限制
**优先级:** P0

**全局限制:**
- 100 次搜索/天（所有用户共享）
- 使用 Firestore `search_usage` 集合追踪

**成本追踪:**
- 前 100 次免费
- 超出后每次 $0.005
- 记录 userId, query, timestamp, estimatedCost

**行为:**
- 达到限制时返回错误消息
- 前端显示友好提示

#### FR-2.2.3 Error Handling | 错误处理
- API 错误：返回降级消息，继续对话
- 网络超时：3 秒超时
- 无效 API Key：记录警告，返回错误

---

### 2.3 Content Fetching | 内容获取

#### FR-2.3.1 Multi-tier Fallback Chain | 多层回退链
**优先级:** P0

按顺序尝试以下方法获取网页内容：

1. **Cache** - 检查内存缓存（TTL: 1 小时）
2. **Direct Fetch** - 直接获取（8 个 User-Agent 轮询）
3. **Jina.ai Reader** - 通过 `https://r.jina.ai/{url}` 获取
4. **Archive.org** - 从互联网档案馆获取历史快照

**要求:**
- 每个 URL 最多尝试所有 4 层
- 使用 Cheerio 提取文本内容（去除脚本、样式）
- 限制内容长度为 10,000 字符

#### FR-2.3.2 Concurrent Fetching | 并发获取
- 并发度: **2**（同时获取 2 个 URL）
- 超时: 8 秒/URL
- 只获取前 **3 个搜索结果**的内容

---

### 2.4 Content Extraction | 内容提取

#### FR-2.4.1 AI-Powered Extraction
**优先级:** P0

使用 DeepSeek AI 从网页内容中提取相关信息。

**输入:**
- rawContent: 网页文本
- userQuestion: 用户原始问题
- targetInfo: 目标信息列表

**输出 (JSON):**
```json
{
  "title": "提取的标题",
  "summary": "简洁摘要（2-3 句）",
  "keyPoints": ["要点1", "要点2", "要点3"],
  "relevanceScore": 8.5,
  "confidence": 0.9
}
```

#### FR-2.4.2 Ranking | 排序
按 `relevanceScore` 降序排列提取的内容。

#### FR-2.4.3 Deduplication | 去重
如果多个页面提取出相似内容（summary 相似度 > 80%），只保留评分最高的。

---

### 2.5 Iterative Search | 迭代搜索

#### FR-2.5.1 Reflection Mechanism | 反思机制
**优先级:** P1

每轮搜索后，AI 反思内容是否充分。

**输入:**
- extractedContent: 已提取的内容
- targetInfo: 目标信息
- userQuestion: 用户问题

**输出 (SearchReflection):**
```json
{
  "sufficient": false,
  "missingAspects": ["定价信息", "用户评价"],
  "refinedQuery": "iPhone 16 price reviews 2024",
  "reasoning": "缺少定价和真实用户评价",
  "confidence": 0.8
}
```

#### FR-2.5.2 Iteration Control | 迭代控制
- **最多 3 次迭代**
- 如果 `sufficient: true`，停止迭代
- 如果达到最大迭代次数，使用现有内容

#### FR-2.5.3 Query Refinement | 查询优化
使用反思中的 `refinedQuery` 作为下一轮搜索查询。

---

### 2.6 Context Building | 上下文构建

#### FR-2.6.1 Final Context Format
将搜索结果和提取内容格式化为 Markdown 字符串。

**结构:**
```markdown
**Web Search Results (实时网络搜索结果):**

[Source 1: title](url)
Summary: ...
Key Points:
- Point 1
- Point 2

[Source 2: title](url)
...
```

#### FR-2.6.2 Memory Integration
如果存在相关记忆，将记忆添加到上下文前部：
```markdown
**User Context (from memory):**
- Memory fact 1
- Memory fact 2
```

---

### 2.7 Progress Reporting | 进度报告

#### FR-2.7.1 Real-time Updates
通过 SSE (Server-Sent Events) 推送进度。

**事件类型:**
- `web_search_start`: 开始搜索
- `web_search_iteration`: 开始第 N 次迭代
- `web_search_fetching`: 获取内容
- `web_search_extracting`: 提取内容
- `web_search_reflecting`: 反思中
- `web_search_complete`: 完成

**数据格式:**
```json
{
  "type": "web_search_iteration",
  "data": {
    "iteration": 2,
    "query": "refined query",
    "message": "Searching: refined query (iteration 2/3)"
  }
}
```

---

## 3. Non-Functional Requirements | 非功能需求

### 3.1 Performance | 性能

| 指标 | 要求 |
|------|------|
| 提示词分析 | < 2 秒 |
| 单次搜索 | < 3 秒 |
| 内容获取（3 个 URL）| < 10 秒 |
| 内容提取（3 个页面）| < 8 秒 |
| 反思 | < 3 秒 |
| **总耗时（3 次迭代）** | **< 90 秒** |

### 3.2 Reliability | 可靠性
- 搜索成功率: > 95%
- 内容获取成功率: > 80%（至少 1 个 URL）
- 降级策略: 如果全部失败，AI 基于预训练知识回答

### 3.3 Scalability | 可扩展性
- 支持添加新的内容获取来源（如 ScrapingBee）
- 支持切换搜索引擎（如 Bing API）

### 3.4 Cost Control | 成本控制
- Tavily Search: 按使用量计費（比 Google Custom Search 便宜）
- DeepSeek API 调用:
  - 提示词分析: 1 次
  - 内容提取: 最多 9 次（3 次迭代 × 3 个页面）
  - 反思: 最多 3 次
  - **总计: 最多 13 次 DeepSeek 调用/对话**

### 3.5 Security | 安全性
- API Key 存储在环境变量
- 搜索查询清理（防止注入）
- URL 验证（只允许 http/https）

---

## 4. User Experience | 用户体验

### 4.1 Transparency | 透明性
- 用户可见搜索进度（前端显示 "🌐 Searching the web..."）
- 显示搜索来源（带链接）

### 4.2 Control | 控制
- 用户无法手动触发/禁用搜索（自动判断）
- 未来可考虑添加设置选项

### 4.3 Error Messages | 错误消息
- 速率限制: "已达到每日搜索限制（100 次），请明天再试"
- API 错误: "网络搜索暂时不可用，我将基于现有知识回答"

---

## 5. Constraints & Limitations | 约束与限制

### 5.1 Technical Constraints
- 依赖 Tavily Search API（需要 API Key）
- 某些网站可能阻止爬取（需要 Jina.ai 或 Archive.org）
- 内容提取质量取决于 DeepSeek 模型性能

### 5.2 Business Constraints
- Tavily 按需付費（更简单的成本模型）
- 不支持图片搜索、新闻搜索（只有普通网页搜索）

### 5.3 Out of Scope | 不包含
- 深度研究（Deep Research）- 使用 Interactions API
- 文件内搜索（如 PDF）
- 实时视频/音频搜索

---

## 6. Success Criteria | 成功标准

### 6.1 Functional Success
- ✅ 95% 的"最新信息"请求被正确识别
- ✅ 80% 的搜索返回至少 1 个相关结果
- ✅ 反思机制减少不必要的迭代（平均 1.5 次/对话）

### 6.2 Quality Metrics
- 提取内容相关性评分 > 7.0（平均）
- 用户满意度 > 4.0/5.0

### 6.3 Technical Metrics
- 平均响应时间 < 30 秒（单次迭代）
- 错误率 < 5%

---

## 7. Future Enhancements | 未来改进

### 7.1 High Priority
- [ ] 支持多语言搜索（中文、日文等）
- [ ] 缓存提取内容（减少重复提取）
- [ ] 用户可选的搜索设置

### 7.2 Medium Priority
- [ ] 新闻搜索集成
- [ ] 图片搜索（结合 Vision API）
- [ ] 本地知识库优先（如果有相关文档）

### 7.3 Low Priority
- [ ] 多搜索引擎聚合（Google + Bing）
- [ ] 高级过滤（日期范围、域名限制）

---

## 8. Glossary | 术语表

| 术语 | 定义 |
|------|------|
| **Prompt Analysis** | 使用 AI 分析用户输入，判断需求 |
| **Iterative Search** | 多轮搜索 + 反思的循环流程 |
| **Content Extraction** | 从网页文本中提取相关信息 |
| **Reflection** | AI 判断内容是否充分的过程 |
| **targetInfo** | 用户想要了解的具体方面 |
| **Fallback Chain** | 多层内容获取策略 |
| **SSE** | Server-Sent Events，实时推送技术 |

---

## 9. References | 参考资料

- [WEB_SEARCH_DESIGN.md](./WEB_SEARCH_DESIGN.md) - 技术设计文档
- [WEB_SEARCH_TASKS.md](./WEB_SEARCH_TASKS.md) - 任务分解文档
- [Tavily Search API Docs](https://tavily.com/api)
- [DeepSeek API Docs](https://api-docs.deepseek.com/)
- [Jina Reader API](https://jina.ai/reader)

---

## 10. Approval | 批准记录

| 角色 | 姓名 | 日期 | 签名 |
|------|------|------|------|
| Product Owner | - | 2024-12-19 | ✅ |
| Tech Lead | - | 2024-12-19 | ✅ |
| Implemented | ✅ | 2024-12-19 | - |
